The first line contains an integer $n$ to indicate the number of denominations in
the currency system.
The second line contains $n$ integers $c_1,\ldots,c_n$ where the currency 
system has $\$c_1,\ldots,\$c_n$.
